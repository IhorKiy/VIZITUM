version: 2

updates:
  # One npm entry, not two: `apps/web` is a workspace of the root manifest and
  # has no lockfile of its own, so the root directory covers both.
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Kyiv
    open-pull-requests-limit: 5
    groups:
      # Patch and minor bumps arrive as one PR per week rather than fifteen.
      # The point of this file is that somebody reads the updates; fifteen PRs
      # is how that stops happening.
      routine:
        update-types:
          - minor
          - patch
    ignore:
      # Prisma's client is generated from the schema and its version is pinned
      # alongside migrations — an unattended bump here lands as a schema-engine
      # change nobody asked for. Upgrade these deliberately, together.
      - dependency-name: prisma
        update-types: ["version-update:semver-major"]
      - dependency-name: "@prisma/*"
        update-types: ["version-update:semver-major"]
      # A TypeScript major cannot install: typescript-eslint caps its peer
      # range below the next one (8.65.0, the latest as of 2026-08-02, asks
      # for `>=4.8.4 <6.1.0`), so `npm ci` fails on ERESOLVE before any check
      # runs — #194 failed all three that way. Nothing here can fix it; the
      # bump becomes possible when typescript-eslint ships support, and this
      # entry should be removed then.
      - dependency-name: typescript
        update-types: ["version-update:semver-major"]
    labels:
      - dependencies

  # The workflow's own actions pin to major tags (actions/checkout@v4), so
  # these are rare and worth seeing when they happen.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    groups:
      # Grouped for the same reason the npm updates are, and the omission was
      # caught the hard way: enabling this file produced three separate PRs to
      # bump three GitHub-maintained actions by a major each. That is one
      # decision — "move the workflow to the current actions" — split across
      # three reviews, which is how a review turns into a rubber stamp.
      actions:
        patterns:
          - "*"
    labels:
      - dependencies
